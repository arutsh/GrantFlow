Workflow rule: one task group = one GitHub ticket = one PR, merged before the next group starts.

## 0. Prod prerequisite (found via live incident, 2026-08-25) — blocks rollout, no dependency on 1-5

- [x] 0.1 Add `ANTHROPIC_API_KEY` to the required prod GitHub Actions secrets list in `docs/deployment/DEPLOYMENT_MODES.md`. (Also wired `ANTHROPIC_API_KEY` through `.github/workflows/deploy.yml`'s `env:`/`envs:`/`VARS` — the docs claim was false without it.)
- [ ] 0.2 Set `ANTHROPIC_API_KEY` as a GitHub Actions secret and confirm it lands in `services/ai/.env.ai.prod` after the next deploy (`docker compose exec ai python -c "import os; print(bool(os.environ.get('ANTHROPIC_API_KEY')))"` should print `True`).
- [ ] 0.3 Re-verify `/ai/extract-budget-excel` recovers for a zero-key org in prod (currently broken — see design.md Deployment Prerequisite).

## 1. Schema and model foundation

- [x] 1.1 Alembic migration: drop `uq_user_provider_keys_customer_provider`; add `label` (nullable `String`) and `is_default` (`Boolean`, `server_default=false`) to `user_provider_keys`; add a partial unique index on `user_provider_keys.customer_id WHERE is_default`.
- [x] 1.2 In the same migration, backfill: for every `customer_id` with exactly one existing row, set `is_default = true` on that row.
- [x] 1.3 New Alembic migration: create `customer_ai_defaults` table (`customer_id` GUID primary key, `platform_fallback_enabled` `Boolean` `server_default=false`, `updated_at`).
- [x] 1.4 Update `UserProviderKey` model (`app/models/user_provider_key.py`) with `label` and `is_default` columns; add `CustomerAiDefaults` model (`app/models/customer_ai_defaults.py`).
- [x] 1.5 Add `claude-haiku-4-5` and `claude-opus-4-5` — superseded during review: a flat `AIModelName` enum let any model be paired with any provider (e.g. claude-haiku-4-5 with ollama). Replaced with a proper `ai_provider_models` catalog table (`provider_id` FK + `name` + `display_name`), migration `016_ai_provider_models.py` seeding it directly (no admin UI/CRUD, per user direction), `app/crud/ai_provider_model.py:exists_for_provider()` used by `POST /ai/settings/keys` to reject a model that doesn't belong to the given provider. `AIModelName` enum and `UserProviderKey.model` property removed (dead once the catalog table is the source of truth).
- [~] 1.6 Run `services/ai` migrations against a local DB and confirm existing single-key customers backfill to `is_default=true`; run `services/ai` test suite and lint clean; PR merged. (migrations+tests+lint done locally; PR not opened — no commit/push without explicit approval)

## 2. CRUD and resolution rewrite — depends on 1

- [x] 2.1 Rewrite `app/crud/user_provider_key.py`: `list_for_customer(customer_id)`, `create(customer_id, user_id, provider_id, label, encrypted_key, model_name, base_url, is_default)`, `set_default(customer_id, config_id)` (transactional: unset current default, set new one), `delete(customer_id, config_id)`.
- [x] 2.2 Implement the delete-default guard in `delete`/a wrapping service function: reject deletion of the current default unless caller supplies `new_default_id` (must belong to the same customer) or `fallback_to_platform=True`; deleting an org's last remaining config is always allowed with no default set afterward. — **Corrected during review:** an AI config was never actually mandatory to use the app, so "rejecting" deletion of the last/default config was solving a problem that didn't exist. `delete()` no longer rejects anything: `new_default_id` is purely an optional convenience to promote a named replacement in the same call; `fallback_to_platform` and `DeleteDefaultRequiresReplacementError` are removed entirely. Enabling the platform fallback is unrelated to deleting a key — it's its own superuser-only toggle (`PUT /ai/settings/platform-fallback`, task 3.2), independent of whether the org has any BYOK config at all.
- [x] 2.3 Add `app/crud/customer_ai_defaults.py`: `get(customer_id)`, `set_platform_fallback(customer_id, enabled)`.
- [x] 2.4 Update `get_active_key_for_customer` (`app/services/provider.py`) to resolve the `is_default=true` row instead of most-recently-updated. (Function lives in `app/crud/user_provider_key.py`, as before; `provider.py` calls it unchanged.)
- [~] 2.5 Update `get_resolved_model` (`app/services/provider.py`): when no default `UserProviderKey` exists, check `customer_ai_defaults.platform_fallback_enabled`; if set, return `resolve_platform_funded_model()`; otherwise return `None` as today. (Deliberately NOT implemented as written — see design-conflict note below.)
- [x] 2.6 Unit tests: multiple configs per provider, default switching, delete-default-without-replacement-allowed, delete-default-with-replacement, delete-last-config-allowed, resolution prefers `is_default` over recency, resolution falls through to platform-funded model only when the flag is set.
- [~] 2.7 Run `services/ai` test suite and lint clean; PR merged. (tests+lint clean locally — 112 passed; PR not opened, no commit/push without explicit approval)

**Design conflict found and resolved with the user during apply (see task 2.5, 3.3, 3.4):** `get_resolved_model` is a single dependency shared by `/ai/decide`, `/ai/parse-budget/stream`, AND `/ai/extract-budget-excel`. Making it internally check `platform_fallback_enabled` (as 2.5/3.3 literally specify) would make excel's own unconditional fallback branch stop firing once a superuser enables the flag for an org — silently mislabeling those calls `funding_source="byok"` and skipping excel's platform rate limit, which task 3.5 explicitly requires NOT to happen. Resolved (user picked this option): `get_resolved_model` stays BYOK-only; added a separate `resolve_with_platform_fallback(customer_id)` in `app/services/provider.py`, called explicitly by `decide_routes.py`/`parse_routes.py` only when `resolved is None`. Excel is untouched and unaffected by the flag either way.

## 3. Superuser-gated platform fallback endpoint — depends on 1, 2

- [x] 3.1 Add `_require_superuser` check (role must be exactly `superuser`) alongside the existing `_require_admin` in `app/api/settings_routes.py`.
- [x] 3.2 Add `PUT /ai/settings/platform-fallback` (superuser only) toggling `customer_ai_defaults.platform_fallback_enabled` for the acting user's resolved customer.
- [~] 3.3 Update `app/api/decide_routes.py`: on `resolved is None`, still return 503 `no_provider` (unchanged) — the fallback now happens inside `get_resolved_model` itself (task 2.5), so no route-level change needed beyond confirming the dependency covers it. (Diverged per the resolved design conflict: decide_routes.py DOES have a route-level change — falls back to `resolve_with_platform_fallback(customer_id)` when `resolved is None`, then 503s only if that's also None.)
- [~] 3.4 Update `app/api/parse_routes.py` similarly — confirm the `unavailable` SSE event now only fires when neither a BYOK key nor an enabled platform fallback resolves. (Same route-level change as 3.3, in parse_routes.py.)
- [x] 3.5 Confirm `app/api/excel_extraction_routes.py` is unchanged — it already calls `resolve_platform_funded_model()` unconditionally and must keep doing so regardless of `platform_fallback_enabled`. (Untouched; confirmed unaffected by construction per the design-conflict resolution above.)
- [x] 3.6 Tests: admin gets 403 on the platform-fallback endpoint; superuser succeeds; `/ai/decide` and `/ai/parse-budget/stream` use the platform model once enabled and fail closed when not; `/ai/extract-budget-excel` behavior unchanged regardless of the flag.
- [~] 3.7 Run `services/ai` test suite and lint clean; PR merged. (tests+lint clean locally; PR not opened)

## 4. Settings API surface — depends on 2, 3

- [x] 4.1 Replace `GET /ai/settings` with a response listing all configs for the customer (`id`, `provider`, `label`, `model`, masked key/base_url, `is_default`) plus the customer's `platform_fallback_enabled` flag.
- [x] 4.2 Add `POST /ai/settings/keys` (create a config; admin-or-superuser) replacing the old provider-keyed `PUT /ai/settings`.
- [x] 4.3 Add `POST /ai/settings/keys/{id}/default` (admin-or-superuser; sets an existing BYOK config as default — does not touch `platform_fallback_enabled`).
- [x] 4.4 Add `DELETE /ai/settings/keys/{id}` (admin-or-superuser) accepting optional `new_default_id`; remove the old `DELETE /ai/settings/{provider}/key` route. — **Corrected during review** along with task 2.2: `fallback_to_platform` removed from this route (no longer a delete-time concept, no longer superuser-gated on delete); always admin-or-superuser now, matching the other key-management routes.
- [x] 4.5 Update `shared/ai_client` or any internal client referencing the removed routes, if any. (Found and updated `services/users/app/api/ai_settings_routes.py` — a `/users/me/ai-settings` proxy forwarding to the old routes; not mentioned in design.md's Impact section.)
- [x] 4.6 Tests: full CRUD flow through the new routes including the delete-default guard's 4xx response shape.
- [~] 4.7 Run `services/ai` test suite and lint clean; PR merged. (tests+lint clean locally; PR not opened)

## 5. Frontend: multi-key settings UI — depends on 4

- [x] 5.1 Update `frontend-typescript/src/api/aiSettingsApi.ts` for the new list/create/set-default/delete-by-id endpoints and response shape.
- [x] 5.2 Rebuild `AiIntegrationsSection.tsx` per the approved mockup: list of configs with a default badge/"Set as default" action, an "Add key" modal, and a delete confirmation flow with an optional replacement picker. (Initially built without a mockup — none was in the change dir. User later shared the actual Claude Design canvas artifact mid-session (Main/AddKey/DeleteConfirm artboards); rebuilt to match it exactly — radio-style default indicator, provider icon, segmented provider picker, masked-key styling, icon-only delete. The always-on-fallback info banner text in the mockup was inaccurate for this change's actual gated behavior — user asked to replace it with copy pointing non-superusers to contact a superuser instead. **Corrected during review** along with task 2.2: the delete modal's platform-fallback radio option was removed — deleting a key is unconditionally allowed, and platform fallback is already its own independent toggle on `PlatformFallbackNote`.)
- [x] 5.3 Add `claude-haiku-4-5` and `claude-opus-4-5` to the frontend's mirrored model list, matching task 1.5. (Mirrored as `MODELS_BY_PROVIDER` keyed by provider name, not a flat list — matches the `ai_provider_models` catalog fix, and the AddKey modal's model dropdown now filters to the selected provider.)
- [x] 5.4 Gate the platform-fallback option in the delete-confirmation and settings UI to render only for users with the `superuser` role; non-superusers seeing an org whose default is already platform-funded see it as read-only info, not an editable choice.
- [~] 5.5 Manually verify in the running app: add two configs for the same provider, switch default, attempt to delete the default without a replacement (rejected in UI), delete the default with a replacement chosen, and (as superuser) enable the platform fallback. (Verified live end-to-end through the real running gateway→users-service→ai-service stack via curl, using minted JWTs for two real dev DB accounts, not mocks — every scenario above passed. Found and fixed a real bug this way: deleting the default with a named replacement hit a partial-unique-index violation from SQLAlchemy's flush ordering — see app/crud/user_provider_key.py delete(). Did NOT complete pixel-level browser verification: an `npx tsc -b` run I did earlier for typecheck polluted frontend-typescript/src with 140 stray compiled `.js` files, which I deleted, but the already-running Vite dev server's module transform cache is now stuck serving a phantom cached `.js` module — it needs a restart to recover, and I didn't restart the user's dev server myself. See final report.)
- [~] 5.6 Run frontend lint/typecheck/tests clean; PR merged. (lint clean on touched files — 85 pre-existing unrelated errors elsewhere; typecheck clean via `npx tsc --noEmit` against the root tsconfig.json, which has the `@/*` path alias — NOT `tsc -b`, which lacks a real outDir here and pollutes src/, see [[feedback_tsc_b_pollutes_frontend_src]]; 287 vitest tests pass — an earlier report of "574 passing" was double-counted because stray `.js` build artifacts sitting next to their `.tsx` sources were being picked up as duplicate test files at that time; PR not opened)
