# Tasks

Workflow rule: **one group = one GitHub ticket = one PR, merged before the next group starts.** Branch names are fixed per ticket. Every PR: `flake8 --max-line-length=100` clean; commits/pushes only with explicit user approval.

## 1. Safety net — ticket #87 (`Platform/Issue-87/chat-migration-safety-net-tests`) — ✅ merged 2026-07-13

- [x] 1.1 Add `services/ai/tests/test_chat_routes.py`: 429 path, `unavailable` when no provider, `thinking→text→done` frame sequence, `X-Session-Id` header, session create vs reuse *(create/reuse/foreign-id semantics covered at CRUD level in 1.2; route test adds the full tool-call frame sequence instead)*
- [x] 1.2 Add `services/ai/tests/test_chat_session_crud.py`: get-or-create (foreign id → new), 50-message cap ordering, `save_turn` round-trip
- [x] 1.3 Add `services/users/tests/test_chat_proxy.py`: body/auth/X-Session-Id forwarding, ConnectError→503, upstream 4xx passthrough
- [x] 1.4 Add `services/budget/tests/test_ai_stream_proxy.py`: mocked ai SSE → `progress`/`created`/`error` re-emission + `create_budget_with_lines_service` invocation *(+ split-frame buffering test — moves to chat with the parse proxy in group 10)*
- [x] 1.5 Fix `.github/workflows/budget.yml` to run all of `tests/`; repair or xfail the 4 currently-unrun files *(all 4 passed as-is; no repairs needed)*
- [x] 1.6 PR merged
- Also landed with #87, reusable by later groups: `ValidUserFactory` (shared/tests/factories), `make_client` fixtures in ai/budget/users conftests, ai factories (`ResolvedModelFactory`, chat session/message), aiosqlite in-memory DB fixture pattern (template for group 6 conversation-store tests), `OTEL_SDK_DISABLED` guard in shared/observability + conftests (users conftest also stubs import-time `init_db`)

## 2. Frontend URL context + navigate — ticket #88 (`Frontend/Issue-88/url-context-navigate-on-create`) — ✅ merged 2026-07-17

- [x] 2.1 `AIChatPanel.tsx`: derive budget id via `useLocation` + `matchPath("/budgets/:id")`; derive `page` from first path segment; `onDone` → `navigate('/budgets/'+id)` instead of `setChatBudgetId` *(also fixed sidebar `<a href>` → `Link`/`NavLink` in `DashboardLayout.tsx` — plain anchors were full-reloading the SPA and dropping chat state on every nav)*
- [x] 2.2 Remove `contextBudget`/`chatBudgetId` from `AiChatContext.tsx`; remove the `setContextBudget` effect in `SingleBudgetView.tsx`; keep `ChatMessage`/`WELCOME_MESSAGE` exports *(also dropped the now-redundant `SingleBudgetViewContainerInner` wrapper — it existed only to feed the deleted effect)*
- [x] 2.3 Vitest coverage: URL→context derivation (`/budgets/<uuid>` vs `/budgets` vs `/dashboard`), navigate-on-done (mocked `streamAiChat`) *(new `AIChatPanel.test.tsx`, 5 tests; added a `scrollIntoView` jsdom polyfill to `setupTests.ts` to make the panel renderable in tests)*
- [x] 2.4 Verify wire format unchanged (still `context_budget_id` to `/users/me/chat/stream`); PR merged *(verification done — wire format confirmed unchanged in `budgetApi.ts`, full lint/typecheck/test suite green, no regressions across all 4 test files; PR #124 merged 2026-07-17, squashed to main as `2be5160`)*

## 3. shared/ai_client — ticket #89 (`Shared/Issue-89/ai-client-library`) — ✅ merged 2026-07-17

- [x] 3.1 Create `shared/ai_client/{__init__,schemas,errors,client}.py` per design (ToolDef/ChatTurn/ToolCall/Reply, AiClient.decide, error taxonomy, connect-only retries) *(also added configurable `backoff_seconds`, not just `connect_retries`, per 2026-07-17 decision; `AiUnavailableError`/`AiRateLimitedError` subclass `AiClientError`)*
- [x] 3.2 Add `shared/tests/test_ai_client.py` with `httpx.MockTransport`: decision parsing, 503/429 mapping, connect-retry, no read-timeout retry *(8 tests, all passing; also added `shared/tests/conftest.py` with the `anyio_backend` fixture — didn't exist yet for this directory)*
- [x] 3.3 Confirm all service CI pipelines pass on the `shared/**` change; PR merged *(discovered none of ai.yml/budget.yml/users.yml/worker.yml actually run `shared/tests/` — each only lists `shared/**` as a trigger filter, "Run tests" stays scoped to its own `services/<name>/tests`; added new `.github/workflows/shared.yml`, path-filtered on `shared/**`, installing ai's requirements.txt (already has httpx/pydantic/anyio/fastapi/pytest) and running black+flake8+pytest against `shared/ai_client`+`shared/tests`; verified locally AND in real CI — `shared-test` check passed on PR #125; PR #125 merged 2026-07-17)*

## 4. ai /ai/decide — ticket #90 (`AI/Issue-90/decide-endpoint`) — HIGH-RISK — ✅ merged 2026-07-18

- [x] 4.1 Add `services/ai/app/api/decide_routes.py` + register router (contract per `specs/ai-decide/spec.md`) *(also added `DecideRequest` to `shared/ai_client/schemas.py` as the FastAPI request model — completes the "single source of truth" decision from #89; `AiClient.decide()` updated to build its payload via the same class instead of a hand-rolled dict, wire format unchanged, all 8 shared tests still pass)*
- [x] 4.2 Add `services/ai/app/services/decide_service.py`: per-request agent, deferred toolset from `available_tools` (fallback: generic structured output — same contract) *(primary path — `ExternalToolset`/`DeferredToolRequests`/`output_type=[str, DeferredToolRequests]` — works cleanly on the pinned pydantic-ai 2.1.0, confirmed empirically before writing; fallback not needed. History bridge (`ChatTurn` → `ModelRequest`/`ModelResponse`) is a small per-turn translation, no caching, rebuilt fresh every call. Hardened after `/code-review high`: a model returning >1 tool call in a turn now raises `UnexpectedModelBehavior` instead of silently dropping the extras; `_RetryableExternalToolset` gives malformed tool-call args a retry budget, but only for self-hosted providers (`ollama`) — `ExternalToolset` hardcodes `max_retries=0` per tool regardless of `Agent(retries=3)`, and a retry on a paid BYOK provider (Anthropic) would silently re-bill the customer, so the budget is opt-in per `provider_name` and defaults to 0)*
- [x] 4.3 Tests: `test_decide_routes.py` (tool_call/reply/503/429), `test_decide_service.py` with PydanticAI TestModel/FunctionModel *(11 tests total — routes: 503/429/tool_call/reply/502-model-error via mocked `decide`; service: real `FunctionModel` proving tool schemas reach the model, deferred calls map to `ToolCall`, multi-tool-call raises, history replay count, domain_context folded into the message text)*
- [x] 4.4 Verify `/ai/chat/stream` untouched and green; PR merged *(full `services/ai` + `shared` suite: 120/120 pass, incl. all pre-existing chat_routes/chat_agent/chat_orchestrator tests untouched; flake8 clean at the project's 100-char limit. `/code-review high` run on the diff surfaced 9 findings; fixed: unhandled model exceptions now return 502 `model_error` instead of a bare 500 (`decide_routes.py`), multi-tool-call silently dropped, `ExternalToolset` retry budget dead for external tools, `AiClient.decide()` leaking raw `pydantic.ValidationError` instead of `AiClientError`, a bare `assert` used as control flow, `customer_id` resolution duplicated across 5 files (now `app/utils/security.resolve_customer_id`), and the 429 `HTTPException` block duplicated across 3 routes (now `app/services/rate_limiter.enforce_rate_limit`). Deliberately reverted: an `agent_factory.py` extraction shared between `decide_service.py` and `chat_agent.py` — `chat_agent.py` is deleted whole-cloth in ticket #95 (task 9.1), so sharing infrastructure with it works against this migration's own isolation goal; the small duplication was left in place instead. Committed as `5dadc38`, pushed to `origin/AI/Issue-90/decide-endpoint`; PR #126 opened and merged to `main` as `f17e64d`.)*

## 5. Chat service scaffold — ticket #91 (`Chat/Issue-91/service-scaffold`) — ✅ merged 2026-07-19

- [x] 5.1 `services/chat/` skeleton: FastAPI + `/health` (plain liveness — `200 {"status":"ok"}`, no DB check), Postgres + alembic baseline (env.py/script.py.mako/empty `versions/`, no tables — #92 adds conversations/messages), Dockerfile copying `shared/`, `.env.chat.*` trio *(mirrors `services/ai`'s structure/config-loading pattern exactly, trimmed to what a zero-logic scaffold needs — no Redis, no BYOK/auth deps; async SQLAlchemy engine + explicit `SQLAlchemyInstrumentor` from day one, matching the design.md #92 note and the async-migration direction rather than budget/users' sync pattern. New database `grantflow_chat` — used the corrected spelling per 2026-07-19 decision: existing `grandflow-*` names (the shared DB container/network/host) are left alone, but anything newly created uses `grantflow`. Added to `docker/postgres/init-db.sh`.) Fixed two bugs in the `custom_openapi()` pattern copied from `services/ai`: it unconditionally marked every path as requiring `BearerAuth`, wrongly including the public `/health` (and `/metrics`) — now skipped via an `unauthenticated_paths` set; and `openapi_schema["components"]` assumed the key always exists, which crashed with `KeyError` the moment a service has zero Pydantic-modeled routes (true for chat today, `/health` returns a bare `dict`) — now `.setdefault("components", {})`. Verified by generating the schema directly and confirming `/health` has `security: None`.*
- [x] 5.2 docker-compose entries in `docker-compose.local.yml`/`.prod.yml` only — `docker-compose.dev.yml` needs no service block (dev mode runs backend services on the host, not in Docker; confirmed no other service has one either) and plain `docker-compose.yml` is unused (no script points `$COMPOSE_FILE` at it, `.github/workflows/compose.yml` only validates its syntax) — + `.github/workflows/chat.yml` (mirrors ai.yml) + `deploy.yml` wired with a new `CHAT_DATABASE_URL` secret (env, `envs:` allowlist, `VARS`/file-loop for the prod env-template substitution) *(initially hardcoded the CI value, corrected to `${{ secrets.CHAT_DATABASE_URL }}` per 2026-07-19 review feedback to match `budget.yml`'s convention — the hardcoded form would also have broken CI outright, since `app/db/session.py`'s `make_url()` runs at import time and raises `ArgumentError` on an empty string, which is what an unset secret resolves to. `CHAT_DATABASE_URL` secret created via `gh secret set` — same `postgresql://postgres:postgres@grandflow-db:5432/grantflow_chat` shape as the repo's other prod DB URLs.)*
- [x] 5.3 Routing for `/api/v1/chat/` with SSE tuning + `X-Conversation-Id` exposure (added to `Access-Control-Expose-Headers` alongside `X-Session-Id`) across all three routing layers: `nginx/nginx.conf`, `nginx/nginx-dev.conf` (new `upstream chat { server host.docker.internal:8003; }`), and prod `Caddyfile` — added now rather than deferred to #92/#93 since the other two layers move in this same ticket. `/health` gets its own exact-match location (`/api/v1/chat/health` → `chat:8000/health`, `handle_path` + rewrite in Caddy, same "single fixed endpoint" pattern already used for `/users/me/chat/stream`); the general `/api/v1/chat/` SSE-tuned block is prepared for #92's real endpoints but has nothing behind it yet
- [x] 5.4 Fixed pre-existing `dev.sh` drift: corrupted shebang (`dev#!/bin/bash` — the file couldn't run via `./dev.sh` at all, only `bash dev.sh`), missing "Start AI Service locally" instructions block (never added when ai was scaffolded in #61), and the swapped Users/Budget metrics-port line (both pointed at the wrong port — `/metrics` is served on each service's own port, not a separate one); added chat's own port-8003 blocks alongside. Also fixed `services/chat/entrypoint.sh` not being executable in git (`services/ai/entrypoint.sh` has the same bug — `100644` instead of `100755` — left alone as pre-existing/out of scope, but chat's own file is correctly `100755`); local.yml bind-mounts `services/chat` over the image's `/app`, so a non-executable host file shadows the Dockerfile's `chmod +x` and fails the container at runtime with `permission denied` — caught by the verification below, not by inspection. **Verified end-to-end**: `docker compose config` valid on all 4 compose files; `chat` image builds clean; black/flake8/mypy/pytest all pass; brought up `grandflow-db`+`chat`+`nginx` via `docker-compose.local.yml`, `alembic upgrade head` ran clean (no-op, no migrations yet), and `curl http://localhost:9082/api/v1/chat/health` through the real gateway returned `200 {"status":"ok"}` with `X-Conversation-Id` present in `Access-Control-Expose-Headers`. Torn down after verification (stale local-mode DB volume from before the ai service existed — unrelated, untouched). Committed on `Chat/Issue-91/service-scaffold` (`eb656ae`), pushed, PR #127 merged to `main` as `23a33fc`. Scope was kept to ticket #91 only — `README.md`/`docs/PRODUCT.md`/`.vscode/launch.json` were being edited by the user in parallel (in-flight when this ticket's commit was made) and were deliberately left out of PR #127; they landed separately as `569955b` ("add chat service debugging configurations and update README for chat service migration"), which added the `.vscode/launch.json` "Debug/Attach Chat Service" entries this ticket didn't touch.

## 6. /chat/stream dark launch — ticket #92 (`Chat/Issue-92/chat-stream-dark-launch`) — HIGH-RISK — ✅ merged 2026-07-19

- [x] 6.1 Alembic migration: `conversations` + `messages` tables per `specs/chat-conversations/spec.md` *(`migrations/versions/001_conversations.py`; `app/crud/conversation.py` implements get-or-create with the "stale/foreign/malformed id degrades to a fresh conversation" scenario — a client-supplied `conversation_id` that isn't a valid UUID is caught explicitly rather than raising deep inside the GUID column's bind-param conversion; 50-message history cap)*
- [x] 6.2 Registry behind `list_tools()`/`call_tool()` with the four curated budget tools + REST dispatchers relocated from ai's `chat_agent.py` (`BUDGET_SERVICE_URL` in chat config) *(`app/services/tool_registry.py` is the generic domain-agnostic base — `page_toolsets`/`targeted_tools`/`resource_id_param`/`creating_tools` class attributes, a `relay_domain_errors` decorator collapsing the try/except shape every dispatcher would otherwise repeat; `app/services/budget_tool_registry.py` is the budget-specific subclass: `targeted_tools = {add_budget_line, update_budget, get_budget_summary}`, `resource_id_param = "budget_id"`, `creating_tools = {create_budget}`. Written so a future domain — e.g. reports — adds its own `<domain>_tool_registry.py` without touching the orchestrator.)*
- [x] 6.3 Orchestrator: guards, per-tool param validation, `budget_id` injection from `context_id`, polite error relay; calls ai via `shared/ai_client` *(`app/services/orchestrator.py::run_turn` — resolved eagerly, before the SSE generator starts, specifically so `AiRateLimitedError` can still become a real HTTP 429 rather than an in-stream `error` event; guard/injection/creating-tool logic is driven off the registry's class attributes, nothing budget-specific is hardcoded here)*
- [x] 6.4 `POST /chat/stream` SSE endpoint (protocol per `specs/chat-streaming/spec.md`) + `GET /chat/conversations[,/{id}/messages]` *(`app/api/chat_routes.py` + `app/services/chat_stream.py`; `done` event keeps the `budget_id` field name for frontend wire compatibility per spec, even though everything else in the registry/orchestrator is domain-agnostic; persistence happens before the `text`/`done` frames are yielded, so a save failure can never surface a reply that then silently vanishes from history)*
- [x] 6.5 Tests: endpoint with mocked AiClient + mocked budget HTTP; orchestrator units; conversation create/reuse/foreign-id *(43 tests: `test_chat_routes.py` (8), `test_orchestrator.py` (7), `test_conversation_crud.py` (16), `test_budget_tool_registry.py` (11), `test_health.py` (1))*
- [x] 6.6 Dark-launch verification: curl through the gateway exercises reply, create, guard, and unavailable paths; PR merged *(verified end-to-end against a live Ollama provider through the real gateway: reply, guard, create_budget, add_budget_line, tenant isolation, pagination — all confirmed live, not just unit-tested. Ships dark: nothing calls this yet, frontend still points at the old `/users/me/chat/stream` proxy. Also brought openspec/ planning artifacts under git tracking for the first time — they were gitignored before this commit. Committed as `36ccdac`, PR #128 merged to `main` 2026-07-19.)*

## 7. Frontend cutover — ticket #93 (`Frontend/Issue-93/cutover-chat-stream`) — HIGH-RISK — ✅ merged 2026-07-20

- [x] 7.1 Move `streamAiChat` to `src/api/chatApi.ts`; URL → `/chat/stream`; body → `{message, conversation_id, context_id, page}`; read `X-Conversation-Id`; rename `sessionId` state to `conversationId` *(`streamAiChat`/`AiChatCallbacks` moved out of `budgetApi.ts` entirely, no dead duplicate left behind; `AiChatContext.tsx`'s `sessionId`/`setSessionId` renamed to `conversationId`/`setConversationId` throughout, `AIChatPanel.tsx` call site updated to match; `done` event's `budget_id` field deliberately left unchanged per spec)*
- [x] 7.2 Update ticket-#88 Vitest tests for new URL/fields *(`AIChatPanel.test.tsx`'s `vi.mock` path moved to `@/api/chatApi` in the same commit as the panel's import — avoided the mock-path-mismatch footgun where tests stay green without exercising the real wiring; all 5 tests still pass unchanged since they assert `streamAiChat` call args positionally, not by field name)*
- [x] 7.3 Manual E2E via compose: create → auto-navigate → add line (URL context) → summarise *(exercised end-to-end against a real running frontend + chat + ai stack, across two very different models — local Ollama `llama3.2`/`gemma4:12b` and Claude via BYOK — which is what actually surfaced most of 7.4's findings rather than a scripted happy-path pass. Full sequence confirmed: create_budget → auto-navigate to `/budgets/:id` → conversation persists across the navigation → add_budget_line with URL-derived context_id → attempted line-level edit (correctly declined, no crash, matches the tool-registry backlog) → update_budget (name change) → get_budget_summary — all four tools exercised, no raw ids in any confirmation message, budget view auto-refreshed without a manual reload each time. "soak" wasn't meaningfully applicable given no real users on this ticket yet. PR #132 merged to `main` 2026-07-20 as `86cbff5`; issue #93 closed.)*
- [x] 7.4 Bundled scope, found during manual E2E (7.3) — not originally scoped to this ticket, but touches `services/ai` unrelated to the frontend diff above:
  - **`ai_audit_logs` gap**: `/ai/decide` (ticket #90) never wrote an audit log entry — only the parse-budget flow did, both before and after the migration. Added audit logging to `decide_service.py::decide()`, mirroring `parse_service.py`'s pattern (timing, token usage, success/failure, re-raise on error); `decide_routes.py` now passes `customer_id`/`user_id` through. New `PROMPT_VERSION = "decide-v1"` constant (no external template for this endpoint, unlike parse's Jinja prompts). New requirement in `specs/ai-decide/spec.md`.
  - **Multi-tool-call hard-failure**: manual E2E against a live Ollama `llama3.2` surfaced this live — a single ordinary request ("add coordinator for 12 months salary 2000£") made the model redundantly re-issue `update_budget` alongside the intended `add_budget_line` (likely confused by "12 months" resembling `duration_months`), which is NOT the "user asked two things" case #90's original hardening assumed. Since call order isn't reliably the real intent, executing any of them risks silently doing the wrong one or dropping the one the user wanted. Changed `decide_service.py` to degrade to a plain `Reply` instead of raising `UnexpectedModelBehavior`/502 — logged as a `logger.warning` (visible for observability) but `success=true` in the audit row (no system error occurred). New requirement in `specs/ai-decide/spec.md`. Tests updated: `test_multiple_tool_calls_raises` → `test_multiple_tool_calls_falls_back_to_reply`, plus new audit-logging coverage for both the reply and multi-call paths (`test_decide_service.py`); `test_decide_routes.py`'s 502 test is unaffected (it exercises the still-valid generic `AgentRunError` mapping, unrelated to this specific path).
  - **Added `gemma4:12b` as a supported Ollama model**: research during manual testing suggested small/weak local models are especially prone to the multi-tool-call confusion above, and Gemma 4 is built with native function-calling rather than fine-tuned on top. Added to `AIModelName` enum (`services/ai/app/models/ai_provider.py`) and the frontend's `SUPPORTED_MODELS` list (`Settings.tsx`) so it's selectable alongside `llama3.2`/Claude — gives a lower-confusion option to test against, doesn't replace the graceful-fallback fix above (any model can still occasionally trip this, especially customer-supplied BYOK models).
  - **`create_budget` now sends `status: "ai_draft"`** (`budget_tool_registry.py`) instead of relying on budget's plain-create default of `"draft"` — matches the old ai-driven parse-budget flow's status so chat-created budgets get the same "needs review" treatment (`SingleBudgetView` auto-opens edit mode for `ai_draft`). Test updated (`test_budget_tool_registry.py`).
  - **Budget view now auto-refreshes on chat-driven mutations**: `AIChatPanel.tsx`'s `onActionResult` invalidates the `["budgetDetails", contextId]` react-query cache whenever a tool executes while that budget is in view — previously required a manual page reload to see a chat-added line. New tests in `AIChatPanel.test.tsx`.
  - **Chat history now survives a page reload**: new `fetchLatestConversation`/`fetchConversationMessages` in `chatApi.ts`, consuming the chat service's existing "any-device retrieval" endpoints (`GET /chat/conversations`, `GET /chat/conversations/{id}/messages}`) that had no frontend caller until now. `AiChatContext.tsx` rehydrates `conversationId` + the visible transcript on authenticated app load instead of relying on ephemeral browser state — required adding `useAuth()` as a guard (only hydrate once authenticated) and updating `AIChatPanel.test.tsx`'s render helper to wrap with `AuthProvider`. New `AiChatContext.test.tsx` (4 tests) covers hydration, empty-history, and fetch-failure paths. New requirements in `specs/chat-url-context/spec.md`.
  - **Chat's shared outbound httpx timeout made env-driven**: manual testing against `gemma4:12b` on local CPU-only Ollama hit `httpx.ReadTimeout` against the hardcoded 30s timeout on `services/chat/main.py`'s shared `http_client` (used for both the `/ai/decide` call and budget REST dispatch) — a 12B model's cold-load + inference routinely exceeds that on modest hardware, unlike prod BYOK providers which respond in seconds. New `HTTP_CLIENT_TIMEOUT_SECONDS` setting (`app/core/config.py`, default 30.0); `.env.chat.private.dev`/`.env.chat.private.local` set it to 180, `.env.chat.prod` sets it explicitly to 30 (repo convention of listing every setting per env file rather than relying on the class default).
  - **`add_budget_line`/`update_budget` success messages no longer embed raw ids**: manual testing surfaced `add_budget_line`'s message including `(line id: <uuid>)` — the exact "raw id SHALL NOT be rendered in the chat transcript" rule `create_budget` already follows, just missed on this dispatcher. `update_budget`'s message embedding the (already-in-view) `budget_id` was the same category of issue — a raw UUID reads as an error to a non-technical user, not confirmation. Both now omit the id entirely (`budget_tool_registry.py`); two new regression tests in `test_budget_tool_registry.py` mirroring the existing `create_budget` "never embeds the raw id" test.
  - **`/code-review high` on this branch — 5 findings fixed, 5 deferred** (deferred list: [[project-chat-migration-code-review-backlog]]):
    - Fixed a regression the id-removal fix above introduced: `_call_add_budget_line` dropped its `resp.json()` call entirely along with the no-longer-needed `line_id`, losing the malformed-2xx-body detection that call incidentally provided (a 200 with an unparseable body — e.g. a proxy error page — would now silently report success). Restored the `resp.json()` call (result discarded); new test `test_malformed_2xx_body_reported_as_failure`.
    - Fixed `AIChatPanel.tsx`'s cache-invalidation logic: `create_budget` is a creating_tool, not a targeted_tool, so it can execute while an *unrelated* budget is the active `contextId` — the old code invalidated whatever budget happened to be in view regardless, which was simply wrong for `create_budget` and a wasted refetch for the read-only `get_budget_summary`. Now gated on tool name (`BUDGET_MUTATING_TOOLS = {add_budget_line, update_budget}`); two new regression tests.
    - Fixed a real race in `AiChatContext.tsx`'s history hydration: the panel is interactable independent of the two sequential hydration fetches resolving, so a user sending a message mid-hydration had their local state unconditionally overwritten once `fetchLatestConversation`/`fetchConversationMessages` finally resolved. Both `setMessages`/`setConversationId` now use the functional-update form and only apply the fetched result if nothing has happened locally yet; new regression test simulating the race with a manually-controlled promise.
    - Added `Field(gt=0)` to `HTTP_CLIENT_TIMEOUT_SECONDS` (`app/core/config.py`) — previously a typo'd `0`/negative value in an env file would silently misconfigure the shared outbound client with no startup failure, only live production breakage. New `test_config.py` (3 tests).
    - Extracted `budgetDetailsQueryKey(id)` (`pages/Budgets/queryKeys.ts`) — the `["budgetDetails", id]` react-query key was a duplicated literal across `SingleBudgetViewContext.tsx`, `SingleBudgetView.tsx`, and this ticket's new `AIChatPanel.tsx` usage; a future restructure of that key could silently break the chat-driven invalidation this ticket just added without this.

## 8. Retire users proxy — ticket #94 (`Users/Issue-94/retire-chat-proxy`) — ✅ merged 2026-07-22

- [x] 8.1 Delete `proxy_chat_stream` + `ChatStreamRequest` (keep ai-settings proxies) and `test_chat_proxy.py` *(`ai_settings_routes.py` — removed the route, its request model, and the now-unused `StreamingResponse` import; `Request` stays imported, still used by `save_ai_settings`. Grepped `services/users/app/` clean of "chat" afterward. 21/21 users tests pass, flake8 clean at the 100-char limit.)*
- [x] 8.2 Remove users chat-stream `location` blocks from both nginx confs; verify chat still works through `/chat/stream`; PR merged *(removed the `/api/v1/users/me/chat/stream` block from `nginx/nginx.conf` and `nginx/nginx-dev.conf`. Ticket body only named the two nginx confs, but the prod `Caddyfile` had the exact same dead block — flagged it to the user, who confirmed removing it too; also fixed the two comments left dangling by that removal (`# same reasoning as the chat/stream rule above` → `ai-settings rule above`; the `USER REGISTRATION` comment's "chat/stream and ai-settings rules above" → "ai-settings rule above"). Left the stale pre-existing "(health only for now — SSE endpoints land in ticket #92)" comment on the `CHAT SERVICE` block alone — predates this ticket, not a consequence of this change. Also left `X-Session-Id` in the CORS `Access-Control-Expose-Headers` list on all three routing layers untouched — ai's old chat stack (the only thing that ever set that header) isn't deleted until #95; cleaning it up there is the more natural point. Confirmed via grep that no code/config anywhere references `proxy_chat_stream`/`ChatStreamRequest`/`users/me/chat/stream`, and that all three files' `/api/v1/chat/` + `/api/v1/chat/health` blocks (the real chat-service routes) are untouched. 21/21 users tests pass, flake8 clean at 100 chars. Committed as `667fd5e` on `Users/Issue-94/retire-chat-proxy`; PR #142 merged to `main` 2026-07-22 as `bffaa83`; issue #94 closed.)*

## 9. Retire ai chat stack — ticket #95 (`AI/Issue-95/retire-chat-stack`) — HIGH-RISK — ✅ merged 2026-07-22

- [x] 9.1 Delete `chat_routes.py`, `chat_orchestrator.py`, `chat_stream.py`, `chat_agent.py`, `chat_intent_schema.py`, `crud/chat_session.py`, session/message models + their tests *(also deleted `tests/factories/chat.py` — bundled, not named in the ticket, but its only two consumers were `test_chat_session_crud.py`/`test_chat_routes.py`, both deleted here, leaving it dead weight. Removed the `chat_routes` import + `app.include_router(chat_routes.router, ...)` from `main.py`. Fixed a comment in `decide_service.py`'s module docstring that referenced `chat_agent.build_agent` as a "same pattern as" comparison — reworded to describe the per-request-Agent pattern directly instead of pointing at now-deleted code. Confirmed `app/models/__init__.py` never imported the chat models (they were only reachable via the now-deleted `chat_routes.py` → `crud/chat_session.py` import chain), so no change needed there or in `migrations/env.py`'s `from app.models import *`.)*
- [x] 9.2 Remove `BUDGET_SERVICE_URL` from ai config + env files (grep for stragglers) *(`app/core/config.py` + `.env.ai.prod`/`.env.ai.private.dev`/`.env.ai.private.local`; confirmed `.github/workflows/ai.yml` never referenced it as a secret. `services/chat`'s own `BUDGET_SERVICE_URL` — a different config, chat's actual link to budget — is untouched.)*
- [x] 9.3 Alembic migration dropping `ai_chat_sessions`/`ai_chat_messages` *(new `migrations/versions/006_drop_ai_chat_tables.py`, `down_revision = "005"` — deliberately did NOT touch/delete migrations 004/005 themselves: 005 also adds the still-needed `customer_id` column to `user_provider_keys`, and rewriting history that other environments may have already applied would break their alembic chain. `downgrade()` recreates both tables + indexes exactly as 004/005 originally created them, matching design.md's rollback note ("alembic downgrade recreates empty tables"). Verified with `alembic history` — chain resolves cleanly to a single head, no branching.)*
- [x] 9.4 Verify: grep shows no budget references in ai; full chat E2E green; PR merged *(grepped `app/` for "budget" — only legitimate remaining hits are the parse-budget flow, explicitly out of scope until ticket #96 relocates it to chat; zero `BUDGET_SERVICE_URL`/`chat_agent`/outbound-to-budget references remain. 73/73 tests pass (down from ~120, expected after removing the chat stack's own tests). `flake8 ./app` clean, matching `ai.yml`'s actual lint scope — note the broader `flake8 app/ tests/ main.py migrations/` invocation surfaces one pre-existing E501 in `migrations/versions/001_create_ai_audit_logs.py`, untouched by this change and outside CI's actual scope. Live E2E through the running stack not run — needs the user's dev environment. PR #143 merged to `main` 2026-07-22 as `f630983`; issue #95 closed.)*

## 10. Parse flow → chat; budget AI-free — ticket #96 (`Chat/Issue-96/parse-budget-relocation`)

- [x] 10.1 Add `AiClient.stream_parse_budget` (+ shared SSE-parser tests: split-frame buffering, error/unavailable passthrough) *(new `shared/ai_client/sse.py::iter_sse_frames` — the buffering loop extracted verbatim from budget_routes.py's old proxy, now a standalone reusable async generator with its own dedicated `test_sse.py` (4 tests: single-frame, split-mid-line, raw error/unavailable passthrough, orphaned-data-without-event dropped). New `ParseProgress`/`ParseDone`/`ParseError`/`ParseUnavailable` classes in `shared/ai_client/schemas.py` (`ParseEvent` union, isinstance-discriminated like `AiDecision` rather than a JSON-tagged union — the SSE `event:` name is what selects the class) — `ParseDone.lines` reuses `shared.schemas.budget_with_lines_schema.BudgetLineInput`. `AiClient.stream_parse_budget(text, user_token)` in `client.py`: checks for a 429 first and raises `AiRateLimitedError` (the old proxy never checked status at all — a latent bug, silently swallowing rate-limits into an empty stream, now fixed as a side effect), other non-2xx raises `AiClientError`, both real exceptions the caller must handle before its stream starts. A connect error, however, yields a `ParseError` event rather than raising — deliberately asymmetric, matching the old proxy's actual graceful-degrade behavior byte-for-byte (`event: error\ndata: Connection to AI service failed`). ai's own ``error`` frame is a raw string, not JSON (confirmed against `parse_service.py`) — `ParseError` is built directly from the frame data, no `json.loads`. 7 new tests in `test_ai_client.py` covering all six paths (progress/done, unavailable, raw-error, 429, 5xx, connect-error).)*
- [x] 10.2 Add chat `POST /chat/parse-budget/stream` (consume ai parse stream → budget `POST /budgets/with-lines` → re-emit `progress`/`created`/`error`/`unavailable`) + tests *(new `app/services/parse_budget_stream.py::build_parse_budget_sse_stream` — pure event-to-SSE-frame adapter, unit-testable directly with a fake async iterator of `ParseEvent`s and a mocked httpx client (matches `test_budget_tool_registry.py`'s existing testing style rather than always going through a full route/TestClient); on `ParseDone` calls budget's public `/budgets/with-lines` with the user's own JWT (no `owner_id` in the payload, same as the tool registry's pattern) and re-emits `created`/`error` exactly as the old proxy's inline creation-failure handling did, including the sanitized `detail` field and the malformed-2xx-body check from #93's code-review backlog. Route added to the existing `app/api/chat_routes.py` (same `/chat` prefix as the other endpoints): follows `/chat/stream`'s established "resolve eagerly, before the StreamingResponse starts" pattern — `stream_parse_budget()` is an async generator, so "eagerly" here means priming it with one `__anext__()` first (this is the point the upstream request is actually sent and its status checked), catching `AiRateLimitedError` → real HTTP 429, any other exception → a single-frame `error` stream (mirroring `/chat/stream`'s own fallback), and splicing the primed first event back into the generator so it isn't lost. New `ParseBudgetStreamRequest` schema (`text: str`). 8 new tests in `test_parse_budget_stream.py` (progress/error/unavailable passthrough, done→created, funder-name-defaults-to-empty-string, creation failure, malformed-2xx-body, upstream-4xx, multi-event sequence) + 4 new route-level tests in `test_chat_routes.py` (`TestStreamParseBudget`: missing body 422, 429-before-any-event, unexpected-exception-during-priming, primed-first-event-not-lost) using `patch.object(AiClient, "stream_parse_budget", ...)` since the client lives on `app.state` rather than being an importable module-level function.)*
- [x] 10.3 Frontend: point `streamAiCreateBudget` at `/chat/parse-budget/stream` *(**Discovered during implementation**: `streamAiCreateBudget` and `streamParseBudget` in `budgetApi.ts` had zero callers anywhere in the frontend — no component, modal, or button invoked either one; they read as leftover code from an earlier UI iteration (PR #75) that never got wired up, or was orphaned once `AIChatPanel`'s own `create_budget` chat tool took over budget creation. Flagged to the user, who chose to delete the dead code rather than repoint it. Both functions removed from `budgetApi.ts` along with the now-unused `AI_API_BASE` const, `GATEWAY_BASE_URL`/`getAuthToken` imports (no longer used by anything else in the file), and the `ParseBudgetResponse` type from `types/budget.ts` (its sibling `BudgetLinePreview` stays — still used by `CreateBudgetWithLinesRequest`, which backs the still-live `createBudgetWithLines`). `tsc --noEmit`, `eslint`, and the full Vitest suite (31/31, 6 files) all clean after removal. No new frontend code calls `/chat/parse-budget/stream` — chat's endpoint exists and is tested, but nothing in the UI reaches it yet, same as the rest of this ticket's backend-only nature.)*
- [x] 10.4 Delete `/budgets/ai/stream` + `ai_service_url` from budget; grep shows zero AI references in budget; PR merged *(removed `ai_create_budget_stream_endpoint` + `AiCreateBudgetStreamRequest` from `budget_routes.py`, along with the imports/logger that became dead once it was gone (`httpx`, `json`, `logging`, `traceback`, `urllib.parse`, `StreamingResponse`, `BaseModel`) — `services/ai` itself untouched, this ticket only ever removed budget's *proxy* to it. `ai_service_url` removed from `config.py` + `.env.budget.private.dev`/`.env.budget.prod` (no `deploy.yml`/compose reference existed to clean up). Deleted `tests/test_ai_stream_proxy.py` per its own docstring ("moves into the chat service in chunk 9") — its scenarios are what `test_parse_budget_stream.py` above ports. 73/73 budget tests pass, `flake8 ./app` clean. Grepped `services/budget/app/` for AI references: only unrelated hits remain (`mapping_service.py`'s semantic-mapping feature has its own internal `"AI"` source label, nothing to do with the ai microservice) — zero `ai_service_url`/`httpx`-to-ai/SSE-proxy references anywhere. PR not yet opened — pending user review/approval before commit.)*
- Bundled scope, found during manual dogfooding of #92's live `add_budget_line` tool (not part of #96's own scope, unrelated to the parse-budget relocation — bundled into this PR per user decision rather than opened separately): `category_name`/`description` on the `add_budget_line` tool schema (`budget_tool_registry.py`) had no per-field JSON-schema `"description"` hints, just bare `{"type": "string"}` — a genuinely ambiguous user message ("add budget line assistant under category Staff costs monthly rate 1200 usd") got parsed by a local Ollama model with the category/description swapped. Root cause was the schema giving the model nothing to disambiguate with, not solely the smaller model's fault (a stronger model could plausibly still trip on it, just less often). Added clarifying `"description"` text to both fields with examples. 26/26 `test_budget_tool_registry.py` + full 62/62 chat suite still pass unchanged (no test asserted the old bare schema shape); black/flake8 clean.
  - **Follow-up after retest (still bundled here)**: with the field-description hints alone but `description` still optional, a second dogfooding round showed a different failure mode — unambiguous messages ("add budget line for IT consultancy under the category MISC...") produced lines labeled "MISC" instead of "IT consultancy" twice in a row. Root cause: the model was omitting the optional `description` param entirely, silently triggering `budget_tool_registry.py`'s `params.get("description") or params["category_name"]` fallback, which masked the gap as a wrong-looking-but-"successful" line instead of surfacing it. Fixed at both layers: `description` moved into the JSON schema's `"required"` list *and* the property text now tells the model to write `'MISC'` itself when it can't determine anything more specific (so the fallback text is a deliberate model output, not a silent code default); `AddBudgetLineParams.description` (`app/schemas/tools.py`) changed from `str | None = None` to `str` so a model that still omits it gets a "need more information" clarifying reply via the existing `ValidationError` → `_clarify_message` path (`orchestrator.py`) instead of a masked default; `_call_add_budget_line`'s fallback expression simplified to `params["description"]` since the field is now guaranteed present by validation. Two existing tests updated to supply `description` now that it's required (`test_budget_tool_registry.py::test_injects_budget_id_from_params`, `test_orchestrator.py::test_model_supplied_id_is_ignored_context_id_wins` — both were testing budget_id injection, not param validation, and had no description in their fixture params). New regression test `test_add_budget_line_without_description_yields_clarifying_reply` pins the now-required behavior. 63/63 chat tests pass, black/flake8 clean.

## 11. OpenAPI→MCP bridge — ticket #97 (`Chat/Issue-97/mcp-bridge-registry`)

- [ ] 11.1 In-process FastMCP `from_openapi` over budget's OpenAPI spec (startup fetch + cached fallback), route allowlist to the four endpoints
- [ ] 11.2 Tool transforms: `name`→`budget_name` rename, hide `owner_id`/`funding_customer_id`/`status`, strip `budget_id` + inject from `context_id`; per-turn JWT
- [ ] 11.3 Acceptance gate: ticket-#92 test suite passes unchanged; add schema-parity + spec-fallback tests; PR merged
