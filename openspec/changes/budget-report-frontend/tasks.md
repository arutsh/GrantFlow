Workflow rule: **one group = one GitHub ticket = one PR, merged before the next group starts.** Tickets are already created (see each group's heading); branch per ticket as `Frontend/Issue-<n>/<short-desc>`. Do not start a group until the previous one's PR is merged. Every PR: `npm run lint` (and any equivalent type-check) clean; commits/pushes only with explicit user approval.

Exception to strict ordering: **group 7 (currency ledger, #165) is externally blocked** on a concurrent session's backend ticket #148 (`Budget/Issue-148/currency-ledger`, in the `budget-reports` change), which is specced but not yet implemented. It only depends on group 1 (#159), not on groups 2–6, so it can be picked up whenever #148 lands — including in parallel with or ahead of groups 2–6 — without stalling the rest of this change on someone else's backend timeline.

## 1. Foundation — gateway wiring, shared types, API layer — ticket #159 (`Frontend/Issue-159/budget-report-foundation`)

- [x] 1.1 Add `location /api/v1/reports/`, `/api/v1/report-lines/`, `/api/v1/attachments/` blocks to `nginx/nginx-dev.conf`, mirroring the existing `/budgets/`/`/budget-lines/` blocks (simple `proxy_pass` to the `budget` upstream)
- [x] 1.2 Add the same three `location` blocks to `nginx/nginx.conf`, mirroring its `rewrite ... break` + variable-`proxy_pass` pattern used for `/budgets/`/`/budget-lines/`
- [x] 1.3 Manually verify (once the backend branch with #146/#147 is merged) that `GET /api/v1/reports/by-budget/{id}` and `GET /api/v1/attachments/by-report-line/{id}` reach the budget service through the dev nginx container — confirmed via `curl` through `localhost:8082`: `reports/by-budget`, `report-lines/by-report`, `attachments/by-report-line`, and `attachments/{id}/download-url` all return `401` (reaches the budget service, auth-gated), same as the existing `/budgets/` route
- [x] 1.4 Extend `frontend-typescript/src/pages/Budgets/types/budget.ts`: add `start_date`/`actual_currency` to `Budget`/`BudgetUpdate`/`BudgetPatched`; add `Report`, `ReportLine`, `Attachment`, `ReportStatus` types matching `shared/schemas/report_schema.py`/`report_line_schema.py`/`attachment_schema.py`
- [x] 1.5 Create `frontend-typescript/src/api/reportApi.ts`: `createReport`, `listReportsByBudget`, `getReport`, `updateReport`, `deleteReport`, `submitReport`, `reviewReport`, `reopenReport`
- [x] 1.6 Add report-line calls to `reportApi.ts`: `createReportLine`, `listReportLinesByReport`, `updateReportLine`, `deleteReportLine`
- [x] 1.7 Add attachment calls to `reportApi.ts`: `uploadAttachment` (multipart `FormData`), `listAttachmentsByReportLine`, `downloadAttachment` (blob response, trigger browser download), `deleteAttachment`
- [x] 1.8 Add a small `src/utils/roleAccess.ts` (or extend an existing utils file) with a `getCurrentCustomerId()` helper decoding `customer_id` from the JWT (same `safeDecodeToken` used by `AuthContext`), and `canReviewReport(budget, currentCustomerId)` / `isBudgetOwner(budget, currentCustomerId)` helpers per design.md's UI-only role-gating decision
- [x] 1.9 Run `npm run lint` / type-check clean; this ticket ships no new UI (types + unused API client + nginx routes only) — verify nothing existing regresses; PR merged — `npm run lint` (89 pre-existing errors, none in the 3 new/changed files), `npx tsc --noEmit` clean, `npx vitest run` 31/31 passing; PR #167 merged to main

## 2. Budget confirmation — ticket #160 (`Frontend/Issue-160/budget-confirmation-ui`) — depends on 1

- [x] 2.1 Add a "Confirm Budget" inline action (date picker + button) to `BudgetViewHeader.tsx` or `SingleBudgetView.tsx`, visible only when `budget.status` is `draft`/`ai_draft` and the current user owns the budget
- [x] 2.2 Wire the action to `editBudget(id, { start_date, status: "confirmed" })`, disabled until a date is picked; on success update the budget in the React Query cache (same `setBudget` pattern as `SingleBudgetViewContext`) and show the returned error inline on failure
- [x] 2.3 Add a component test covering: button hidden when already confirmed, disabled with no date picked, calls `editBudget` with the right payload, shows error on failure

Expanded scope (folded in after starting implementation — see design.md's new decisions and specs/budget-confirmation-ui/spec.md's new requirements):

- [x] 2.4 Backend: in `update_budget_service` (`services/budget/app/services/budget_services.py`), allow the confirm transition (`status: "confirmed"` + `start_date` set, from `draft`/`ai_draft`) for either the owner or the customer matching `funding_customer_id` (reuse the `_can_view_budget` check), while every other mutation stays owner-only — implemented via a fallback lookup when the owner-scoped fetch fails; also fixed a latent bug this surfaced: `owner_id` was previously always defaulted to the caller's own `customer_id` and passed through to `update_budget`, which would have silently reassigned ownership to the funder once they could call this endpoint (now `owner_id` stays `None`/unchanged unless a superuser explicitly reassigns it)
- [x] 2.5 Backend: add a `confirmed → draft` transition guard to `update_budget_service` — reject if the budget has any report in `submitted`/`approved`/`rejected` state; on success, delete the budget's draft-state report(s) in the same transaction
- [x] 2.6 Backend: add a `status == confirmed` guard to `budget_line_services.py` (`create_budget_line_service`/`update_budget_line_service`/`delete_budget_line_service`) and to the budget metadata update path — reject the request once the budget is confirmed. (Corrected 2026-07-28: originally implemented as a report-existence check, which left a real gap — a confirmed budget with zero reports yet was still editable. A report can only ever be created against an already-confirmed budget, so "confirmed" is the strictly broader, actually-correct condition; the report-existence check was replaced, not supplemented. Frontend's `hasReports`/reports-fetch was replaced with an `isLocked = budget.status === "confirmed"` derived directly from the already-fetched budget, removing an unnecessary network call.) Also fixed a second latent bug found along the way: `BudgetUpdate.status` inherited `BudgetBase`'s `= BudgetStatus.draft` default, so any PATCH that omitted `status` was silently coerced to `"draft"` by Pydantic (masked until now only because every existing frontend caller happens to always resend `status` explicitly); `BudgetUpdate.status` is now `BudgetStatus | None = None` so omitting it truly means "leave unchanged"
- [x] 2.7 Backend: unit tests for 2.4–2.6 (funder-can-confirm draft/ai_draft, stranger rejected, funder-cannot-edit-metadata, funder-cannot-revert; owner-can-revert-with-no-reports, revert-blocked-by-submitted/approved-report, revert-succeeds-and-deletes-draft-reports; metadata-edit-blocked/allowed by report existence; budget-line create/update/delete blocked by report existence) — `tests/test_budget_confirmation_lifecycle.py`, 14 new tests; full backend suite 172/172 passing, `flake8 --max-line-length=100` clean on all touched files. De-duplicated afterward: the `db` fixture (real sqlite session over Budget/BudgetLine/BudgetCategory/Report/ReportLine) was copy-pasted byte-for-byte from `test_report_routes.py` — moved to `conftest.py` as a shared fixture and removed from both test files (plus their now-unused `create_engine`/`sessionmaker`/`Base`/`ReportLineModel`/`BudgetLineModel`/`BudgetCategoryModel` imports where no longer referenced directly)
- [x] 2.8 Frontend: show budget `status`, `start_date`, and a computed end date (`start_date` + `duration_months`) on the single-budget view outside edit mode; omit dates when `start_date` is unset — also fixed a bug this depended on: `populate_budget_with_user_details` (`services/budget/app/services/budget_services.py`) never included `start_date`/`actual_currency` in the enriched dict returned by `GET /budgets/{id}`, so both fields always serialized as `null` regardless of the real column values, even though `BudgetWithLines` declared them
- [x] 2.9 Frontend: add a "Cancel Confirmation" action visible to the owner only on a `confirmed` budget, calling `editBudget(id, { status: "draft" })`, showing the backend's rejection message inline (via `error.response.data.detail`) when blocked by a non-draft report
- [x] 2.10 Frontend: hide/disable the "Edit" action (with an explanatory message) once the budget is confirmed (`isLocked = budget.status === "confirmed"`, corrected 2026-07-28 from a report-existence check — see 2.6); `start_date` is never exposed as a free-editable field — only settable via the confirm action
- [x] 2.11 Frontend: proposed an interactive mockup (published as an Artifact) unifying `SingleBudgetView`'s view and edit rendering into one persistent layout; signed off by the user 2026-07-27
- [x] 2.12 Frontend: implemented the signed-off unified layout — `BudgetEditMode` deleted entirely; `BudgetViewHeader` now owns its own edit-mode toggle (name/funder/duration inline inputs, status badge, dates row, Confirm/Cancel-Confirmation/Edit-lock actions), auto-opening for `ai_draft` budgets (previously handled by `SingleBudgetView`'s effect); `BudgetViewLinesTable` (already always-mounted, unlike the old duplicate bulk-editor in `BudgetEditMode`) gained a `readOnly` prop driven by `hasReports` instead of needing its own edit-mode branch; `SingleBudgetView` now fetches `listReportsByBudget` to compute `hasReports` and renders Header→Summary→Lines→Traces unconditionally. Follow-up visual pass (2026-07-27) brought the section styling closer to the approved mockup: `BudgetViewHeader`/`BudgetViewSummary`/`BudgetViewLinesTable`/`BudgetViewTraces` switched from flat `bg-gray-50 border-b` sections to white `rounded-xl border shadow-sm` cards on the gray page background, the status badge gained a color dot, the Confirm/Cancel-Confirmation actions got a bordered "action bar" treatment, the locked-note got a lock icon, owner/funder text moved off illegible `text-gray-300` onto `text-slate-500`/`text-slate-700`, and `SingleBudgetView`'s container gained a centered `max-w-4xl` column with consistent `gap-5` spacing instead of full-bleed `px-10`. Second visual pass (2026-07-28) closed the remaining gaps against the mockup: the Confirm/Cancel-Confirmation action bar moved out of the header `Card` into its own sibling element (matching the mockup's layout, not nested inside the header); the shared `Button` component gained real `disabled:opacity-40` styling (previously a disabled button looked identical to an enabled one); `BudgetViewSummary`'s and `BudgetViewLinesTable`'s amounts now go through the existing `formatCurrency` util (`@/utils/currency`, already used by `DonorDashboard`) instead of a bare `toLocaleString()`, so they show the budget's actual currency symbol; the shared `TableCommon`/`Table` component (also used by `DonorDashboard` and `TableView`) gained muted uppercase header-cell typography consistent with the rest of the page, a `bare` prop so an already-carded consumer like `BudgetViewLinesTable` doesn't get a redundant nested shadow, and its category-grouping expand/collapse control switched from a plain `▶`/`▼` glyph (which renders as a colored emoji play-button in some fonts, clashing with the page's neutral palette) to `lucide-react`'s `ChevronRight`/`ChevronDown` icons, which respect `currentColor`. Third visual pass (2026-07-28) toned down the row-action buttons that still clashed with the rest of the (now lighter/card-based) page: `BudgetViewLinesTable`'s per-line "Edit"/"Delete" switched from solid `primary`/`danger` text buttons to `variant="icon"`/`icon-danger"` buttons with `lucide-react`'s `Edit2`/`Trash2`, matching the identical pattern `TableView.tsx` already used elsewhere in the app (so `ConfirmDeleteButton` gained an optional `variant`/`title` prop to support the icon form without changing its default text-button behavior anywhere else); "New Budget Line" moved from `primary` to `secondary`, matching the header's "Edit" button weight; `BudgetViewSummary`'s stat row switched from `flex flex-wrap gap-8` (uneven natural-width gaps) to `grid grid-cols-2 sm:grid-cols-3 gap-x-8 gap-y-4` (even columns), matching the mockup's grid layout
- [x] 2.13 Frontend: component tests for 2.8–2.10 (status/date display incl. omitted-dates case, funder-can-confirm, revert happy path, revert-blocked error, edit action hidden once a report exists, metadata-save payload correctness) — `BudgetViewHeader.test.tsx` expanded to 16 tests; full frontend suite 47/47 passing, `npx tsc --noEmit` clean, lint unchanged at 95 pre-existing problems (0 new)
- [x] 2.14 Run the frontend test suite + `npm run lint` clean, backend tests for 2.7; PR merged — frontend `npx vitest run` 47/47, `npx tsc --noEmit` clean, repo lint at 94 pre-existing problems (no new); backend `pytest services/budget` 184/184, `pytest services/chat` 72/72, `flake8 --max-line-length=100` clean. Merged 2026-07-28 via PR #170 (`Closes #160`) — note: merged without the manual dev-stack exercise the PR description flagged as recommended; worth a quick manual pass opportunistically

## 3. Reports list, creation, and read-only detail — ticket #161 (`Frontend/Issue-161/reports-list-and-detail`) — depends on 1, 2

- [x] 3.1 Add `frontend-typescript/src/pages/Budgets/components/ReportsList.tsx`: fetches `listReportsByBudget(budget.id)` via React Query, renders each report's name/period/status, empty state with "New Report" when none exist — "New Report" gated to `isBudgetOwner`; list itself renders for both owner and funder viewers
- [x] 3.2 Add a "New Report" form/modal (reuse `Modal` from `components/ui`) collecting `name` and optional `period_start`/`period_end`; on submit calls `createReport`, shows the backend's error message inline on rejection (e.g. overlapping period), and on success navigates to the new report's detail route — implemented inline in `ReportsList.tsx` as `NewReportModal`, same pattern as `BudgetConfirmAction` living inline in `BudgetViewHeader.tsx`
- [x] 3.3 Mount `ReportsList` inside `SingleBudgetView.tsx`'s non-edit-mode branch, gated on `budget.status === "confirmed"` or `budget` already having reports — gating lives inside `ReportsList` itself (it always fetches via `reportsByBudgetQueryKey`, then returns `null` unless confirmed or `reports.length > 0`), so `SingleBudgetView` mounts it unconditionally, same shape as the other always-mounted section components
- [x] 3.4 Add a new route `/budgets/:id/reports/:reportId` in `App.tsx` pointing at a new `ReportDetailView` container
- [x] 3.5 Add `frontend-typescript/src/pages/Budgets/ReportDetailView.tsx`: reads `:reportId` from the route, fetches the report (`getReport`) and its lines (`listReportLinesByReport`), renders status, period, and a read-only lines table (line CRUD/actions land in ticket 4) — also fetches the parent budget (`fetchBudgetById`, shared `budgetDetailsQueryKey` cache) to format line amounts in `local_currency` and link back to `/budgets/:id`; added `reportQueryKey`/`reportLinesQueryKey` to `queryKeys.ts` alongside the pre-existing `reportsByBudgetQueryKey`, as separate cache entries so ticket 4's line mutations can invalidate lines without refetching the report
- [x] 3.6 Add tests: `ReportsList` empty state, list rendering, create-report happy path, overlapping-period error surfaced inline; `ReportDetailView` renders report metadata and lines read-only — `ReportsList.test.tsx` (8 tests, incl. hidden-on-draft/shown-on-archived-with-reports gating and owner-only New Report visibility) + `ReportDetailView.test.tsx` (3 tests, incl. error state); extracted shared `ReportStatusBadge.tsx` used by both
- [ ] 3.7 Run the frontend test suite + `npm run lint` clean; PR merged — `npx vitest run` 59/59 passing (11 new), `npx tsc --noEmit` clean, `npm run lint` unchanged at 94 pre-existing problems (0 new); not yet a PR

## 4. Report line CRUD and lifecycle actions — ticket #162 (`Frontend/Issue-162/report-line-lifecycle`) — depends on 3

- [x] 4.1 Add `ReportLineRow.tsx` (or extend the lines table) supporting add/edit/delete of a line (budget line select, description, amount), enabled only while the report is `draft` — line creation is a `Select`-driven `AddReportLineModal` inline in `ReportDetailView.tsx` (same pattern as `ReportsList.tsx`'s `NewReportModal`); `ReportLineRow.tsx` handles per-row inline edit/delete only, matching `budget_line_id` against the already-fetched `budget.lines` for display since `ReportLine` itself only carries the id, not the joined budget-line object
- [x] 4.2 Wire add/edit/delete to `createReportLine`/`updateReportLine`/`deleteReportLine`, invalidating the report-lines query on success — implemented as direct `queryClient.setQueryData(reportLinesQueryKey(reportId), ...)` cache updates (append/replace/filter) rather than a broad invalidate+refetch, mirroring `BudgetViewLinesTable`'s local-cache-update convention on delete
- [x] 4.3 Add "Submit" action (visible to the owner when `status === "draft"`) calling `submitReport`, updating the displayed status on success
- [x] 4.4 Add "Approve"/"Reject" actions (visible when `canReviewReport(budget, currentCustomerId)` is true and `status === "submitted"`), with a review-notes field, calling `reviewReport`
- [x] 4.5 Add "Reopen" action (visible to the owner when `status === "rejected"`) calling `reopenReport`, re-enabling line edits on success — all three lifecycle actions live in one `ReportActions` component in `ReportDetailView.tsx`, sharing one `updateReportCache` merge helper (same bare-scalar-merge pattern as `SingleBudgetView`'s `handleBudgetUpdated`)
- [x] 4.6 Add tests: line CRUD gated by draft status, submit transition, review actions gated by role+status, reopen transition — `ReportDetailView.test.tsx` expanded to 16 tests across 5 describe blocks (metadata/read-only, line CRUD, submit, review, reopen); mocks `@/utils/roleAccess` for the first time in this view (previously relied on the real, unauthenticated-in-tests decode)
- [x] 4.7 **Added beyond the original task list (2026-07-29):** `ReportLine.expense_date` — the user caught that `ReportLine` had no field for when the expense actually happened, only `created_at` (when the row was written). Backend: `services/budget/app/models/report.py` gains `expense_date: Date, nullable=False`; migration `000008_add_report_line_expense_date.py` (add nullable → backfill from `created_at` → set `NOT NULL`, verified up/down against real Postgres, confirmed to touch only 2 pre-existing dev-only rows since this UI had never shipped before); `shared/schemas/report_line_schema.py` (required on create, optional on update); service-layer validation in `report_line_services.py` rejecting unless `period_start <= expense_date <= period_end`; `ReportLineFactory` + 5 new tests in `test_report_line_routes.py` (`TestExpenseDateValidation`) + updates to every existing `ReportLineCreate`/`ReportLineModel` call site across `test_report_line_routes.py`, `test_currency_ledger.py`, `test_budget_confirmation_lifecycle.py`, `test_attachment_routes.py` — 189/189 `services/budget` tests pass, flake8/mypy clean, black clean except 4 pre-existing unrelated drift files (was 3; the 4th's diff predates this change, confirmed via `black --diff`). Frontend: `types/budget.ts`, `AddReportLineModal`'s new required date input (min/max clamped to the report's period, backend stays authoritative — same pattern as upload validation), `ReportLineRow`'s display/inline-edit, `Input.tsx` gained `min`/`max` pass-through props. Amended `openspec/specs/budget-reports/spec.md`'s "Report lines reference a specific budget line" requirement and this change's `specs/budget-report-ui/spec.md`'s "Report line CRUD while draft" requirement, both with dated notes.
- [x] 4.8 **Added beyond the original task list (2026-07-29):** `ReportLine.extra_fields` visibility — no backend/schema change needed (`extra_fields` already existed on the model/schema/types from #146/#148's groundwork, just never had frontend UI). Mirrors `BudgetLine`'s existing extra-fields UX exactly, per the user's explicit "check budget line" instruction: `ReportDetailView.tsx` computes `extraFieldKeys` (union of keys already used across the report's lines, same pattern as `SingleBudgetViewContext`'s `existingExtraKeys`) and renders one dynamic table column per key; `AddReportLineModal` prefills those keys (value empty, key locked/undeletable) and allows adding new key/value rows, same add/remove/lock UX as `AddBudgetLine.tsx`; `ReportLineRow` gained a `extraFieldKeys` prop to render matching read-only cells (editing `extra_fields` on an already-existing line was explicitly out of scope — display + add-time entry only). 2 new tests in `ReportDetailView.test.tsx` (dynamic column rendering, prefill/lock/add-new-key on the form) — 74/74 frontend tests passing, `npx tsc --noEmit` clean, lint unchanged at 94. Amended this change's `specs/budget-report-ui/spec.md` with a dated note + 2 new scenarios.
- [ ] 4.9 Run the frontend test suite + `npm run lint` clean; PR merged — `npx vitest run` 74/74 passing, `npx tsc --noEmit` clean, `npm run lint` unchanged at 94 pre-existing problems (0 new); not yet a PR

## 5. Attachments — ticket #163 (`Frontend/Issue-163/report-attachments-ui`) — depends on 4

- [ ] 5.1 Add `frontend-typescript/src/pages/Budgets/components/AttachmentUpload.tsx`: file input + upload button per report line, client-side validation (15MB max, PDF/JPEG/PNG/HEIC allowlist) showing an inline error before calling `uploadAttachment`, visible only while the report is `draft`
- [ ] 5.2 List each report line's attachments (filename, size) via `listAttachmentsByReportLine`, with a download control (calls `downloadAttachment`, triggers a browser download using the original filename) and a delete control (calls `deleteAttachment`, visible only while `draft`)
- [ ] 5.3 Integrate `AttachmentUpload`/attachment list into `ReportLineRow.tsx` from ticket 4
- [ ] 5.4 Add tests: oversized/disallowed-type file rejected client-side without a network call, successful upload appends to the list, delete removes from the list, controls hidden once the report leaves `draft`
- [ ] 5.5 Run the frontend test suite + `npm run lint` clean; PR merged

## 6. Donor dashboard wiring and end-to-end verification — ticket #164 (`Frontend/Issue-164/donor-dashboard-reports-link`) — depends on 5

- [ ] 6.1 Replace `DonorDashboard.tsx`'s disabled "View Reports" button with a `Link` to that budget's report list (`/budgets/:id` with the Reports section, or directly to `/budgets/:id/reports/:reportId` if the budget has exactly one report — implementation-time call per design.md's open question)
- [ ] 6.2 Update or add to `DonorDashboard.test.tsx` covering the now-enabled "View Reports" link
- [ ] 6.3 Manually exercise the full flow against the running dev stack: confirm a budget → create a report → add lines → upload an attachment → submit → review as funder (approve or reject) → if rejected, reopen and confirm edits are allowed again → view the same flow from the donor dashboard
- [ ] 6.4 Run the frontend test suite + `npm run lint` clean; PR merged

## 7. Currency ledger — funding receipts & conversions — ticket #165 (`Frontend/Issue-165/currency-ledger-ui`) — depends on 1 only; BLOCKED on backend ticket #148

- [ ] 7.1 Before starting: confirm `services/budget` has merged ticket #148 — `funding_receipt_routes.py`/`currency_conversion_routes.py` and `shared/schemas/currency_ledger_schema.py` must actually exist. Do not begin implementation against a nonexistent backend.
- [ ] 7.2 Add `location /api/v1/funding-receipts/` and `/api/v1/currency-conversions/` blocks to both `nginx/nginx-dev.conf` and `nginx/nginx.conf`, mirroring the pattern used for `/reports/` in ticket 1
- [ ] 7.3 Extend `frontend-typescript/src/pages/Budgets/types/budget.ts` with `FundingReceipt` and `CurrencyConversion` types matching `shared/schemas/currency_ledger_schema.py`
- [ ] 7.4 Create `frontend-typescript/src/api/currencyLedgerApi.ts`: `createFundingReceipt`, `listFundingReceiptsByBudget`, `createCurrencyConversion`, `listCurrencyConversionsByBudget`
- [ ] 7.5 Add `actual_currency` as an editable field to `SingleBudgetView.tsx`'s inline edit-mode metadata grid (alongside name/funder/duration), wired through the existing `editBudget` call
- [ ] 7.6 Add `frontend-typescript/src/pages/Budgets/components/CurrencyLedgerPanel.tsx`: shows a "set actual currency first" prompt (linking into edit mode) when `budget.actual_currency` is unset; otherwise shows funding-receipt and currency-conversion recording forms plus a chronological history list, each conversion row showing its implied rate (donor amount ÷ local amount) — no aggregate balance figure, per design.md's explicit non-goal
- [ ] 7.7 In the same panel, show a "received to date" figure: sum `listFundingReceiptsByBudget`'s amounts client-side and compare against `budget.total_amount` as a percentage/progress indicator when `budget.local_currency === budget.actual_currency`; otherwise show the two figures side by side with their own currency labels and no computed ratio, per design.md's currency-safe display decision
- [ ] 7.8 Mount `CurrencyLedgerPanel` inside `SingleBudgetView.tsx`'s non-edit-mode branch, visible only when `isBudgetOwner(budget, currentCustomerId)` is true
- [ ] 7.9 Add tests: prompt shown when `actual_currency` unset, forms shown once set, record-receipt/record-conversion happy paths, history list rendering with correct implied rate per row, received-to-date percentage shown only in the same-currency case (two-figure fallback otherwise), section hidden for non-owners
- [ ] 7.10 Run the frontend test suite + `npm run lint` clean; PR merged
